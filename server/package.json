{
  "name": "projetinho",
  "version": "0.0.1",
  "description": "QSaude's middleware",
  "main": "index.js",
  "scripts": {
    "start": "tsc && node dist/index.js",
    "clean-dist": "rm -rf ./dist",
    "postinstall": "yarn run clean-dist && tsc",
    "reinstall": "rm -rf ./node_modules || true && yarn cache clean && yarn"
  },
  "license": "ISC",
  "dependencies": {
    "ts-node": "8.4.1",
    "express": "4.17.1"
  },
  "devDependencies": {
    "@types/express": "4.17.1",
    "@types/node": "11.9.4",
    "tsconfig-paths": "3.7.0",
    "typescript": "3.6.3"
  },
  "engines": {
    "node": "10.16.3",
    "npm": "6.1.0",
    "yarn": ">=1.10.1"
  }
}